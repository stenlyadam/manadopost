import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import {Header, MenuHeader} from '../../components';

const Latest = ({navigation}) => {
  return (
    <View>
      <Header />
      <MenuHeader />
    </View>
  );
};

export default Latest;

const styles = StyleSheet.create({});
