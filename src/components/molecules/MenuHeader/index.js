import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const MenuHeader = () => {
  return (
    <View>
      <Text>Menu Header Component</Text>
    </View>
  );
};

export default MenuHeader;

const styles = StyleSheet.create({});
